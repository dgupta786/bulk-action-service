/**
 * Maps entity types to their corresponding MongoDB collection names
 */
export const collectionMapping: Record<string, string> = {
  Contact: 'contacts',
  // Add more entity to collection mappings as needed
};